package com.example.wastescanner.ui.theme

import androidx.compose.ui.graphics.Color

// Zapis 0xFF oznacza 100% nieprzezroczystości (nie ruszaj tego)
// Kolejne 6 znaków to standardowy kod koloru HEX (jak w Photoshopie czy CSS)

// --- GŁÓWNE KOLORY EKO-APLIKACJI ---
val GreenPrimary =  Color(0xFF689f38)     // Główny, mocny zielony (przyciski)
val GreenSecondary = Color(0xFF8bc34a)   // Jasny zielony (akcenty)
val GreenTertiary = Color(0xFFAED581)

val offwhite = Color(0xFFD2BBA5)// Bardzo jasny (tła)

// --- KOLORY SYSTEMOWE I TŁA ---
val BackgroundLight = Color(0xFFD59F66)   // Delikatnie zielonkawe tło (jasny motyw)
val BackgroundDark = Color(0xFFD59F66)    // Ciemnoszare tło (ciemny motyw)
val SurfaceDark = Color(0xFF1E1E1E)       // Nieco jaśniejszy szary na karty
val TextDark = Color(0xFF212121)          // Prawie czarny tekst
val TextLight = Color(0xFFFAFAFA)         // Prawie biały tekst

